"""The terminal front end.

    term     raw mode and keypresses
    screen   the frame: exact-width lines and a row-diff repaint
    panels   the three HUD tabs, built from whatever the act returns
    actions  a keypress becomes a decision
    menu     everything you can reach before and between runs
    editor   the save editor
"""
