"""The simulation itself: the run, the clock and the registries.

Nothing here draws anything or touches a file. `state.Game` is the run,
`acts` is what you are doing, `research` is what you are allowed to do,
`projects` is what you have bought and `war` is what is being done to you.
The registries all start empty; content fills them.
"""
